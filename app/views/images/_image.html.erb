<div id="<%= dom_id image %>">
  <p>
    <strong>Name:</strong>
    <%= image.name %>
  </p>

  <p>
    <strong>Author:</strong>
    <%= image.author %>
  </p>
  <% if image.paint.attached? %>
    <img src="<%= url_for(image.paint) %>" >
    nsdsdddddddddddd
  <% end %>


</div>
